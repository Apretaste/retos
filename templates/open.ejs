<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab disabled"><a href="#" class="active">Retos del día</a></li>
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'RETOS DONE'})">Completados</a></li>
		</ul>
	</div>
</div>

<% for(var i in challenges) { var item = challenges[i]; %>
<div class="row">
	<div class="col s12 m6">
		<div class="card">
			<div class="card-content">
				<i class="medium left material-icons"><%= item.icon %></i>
				<span class="card-title <%= item.done ? 'completed' : '' %>"><%= item.name %> (<%= formatCredit(item.prize) %>)</span>
				<p class="clear"><%= item.description %></p>
			</div>
			<div class="card-action">
				<% if(!item.done) { %>
					<a href="#" onclick="apretaste.send({command: '<%= item.command %>', data: {seed: (new Date()).getTime()}})">Completar</a>
					<a href="#" onclick="apretaste.send({command: 'RETOS QUITAR', data: {challenge: <%= item.id %>}})">Quitar</a>
				<% } %>
			</div>

		</div>
	</div>
</div>
<% } %>

<!-- Modal Structure -->
<div id="skip" class="modal">
	<div class="modal-content">
		<h4>¡Cuidado!</h4>
		<p>¿Está seguro que desea cambiar este reto por otro de la lista? Saltar retos tiene un costo de §0.2 crédiros.</p>
	</div>
	<div class="modal-footer">
		<a href="#!" class="modal-close waves-effect waves-green btn-flat">Cerrar</a>
		<a href="#!" class="waves-effect waves-green btn-flat" onclick="apretaste.send({command: 'RETOS SKIP'})">Saltar (§0.2)</a>
	</div>
</div>
