<div class="row">
	<div class="col s12">
		<h1>Retos de la app</h1>
		<p>Complete los siguientes retos y gane <b>§<%= credit.toFixed(2) %></b>.</p>

		<ul class="collection">
			<% goals.forEach(function(goal, i) { %>
				<li class="collection-item">
					<div>
						<% if (goal.completed == 0) { %>
							<a class="secondary-content green" href="#!" onclick="apretaste.send({command: goals[<%= i %>].link})"><i class="material-icons green">send</i></a>
						<% } %>
						<span class="<%= goal.completed==1 ? 'completed' : '' %>"><%= goal.caption %></span>
					</div>
				</li>
			<% }); %>
		</ul>
	</div>
</div>